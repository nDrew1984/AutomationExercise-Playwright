# conftest.py (project root)
# This file is intentionally empty.
# Its purpose is to ensure pytest adds the project root to sys.path,
# so that the "pages" and "tests" packages remain importable
# even when running from any subdirectory (e.g., api-tests/).
